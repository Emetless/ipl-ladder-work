<h1>Articles#index</h1>
<p>Find me in app/views/articles/index.html.erb</p>
<% @articles.each do |article| %>
  <p>
    <%= article.content %>
    <% if @edit %><%= link_to 'edit', articles_edit_path(article_id: article.id) %><% end %>
  </p>
<% end %>